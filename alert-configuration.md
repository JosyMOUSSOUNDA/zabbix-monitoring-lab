# Alert Configuration

Triggers were configured in Zabbix to detect abnormal behavior.

Examples:

CPU usage > 80%  
Memory usage > 85%  
Service unavailable

When these thresholds are exceeded, alerts are generated automatically.
